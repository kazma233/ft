package constants

var (
	// SLine split message
	SLine = []byte("\r\n")
	// DefaultSLenSize SLine size
	DefaultSLenSize = uint64(len(SLine))
)

const (
	// DefaultByteSize default byte size
	DefaultByteSize = 10240
)
